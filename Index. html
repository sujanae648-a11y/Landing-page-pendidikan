```html
<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>EgiShop - Official Store by Muhammad Egi Sujana</title>
    <!-- Tailwind CSS -->
    <script src="https://cdn.tailwindcss.com"></script>
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
    <!-- Phosphor Icons -->
    <script src="https://unpkg.com/@phosphor-icons/web"></script>
    
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        sans: ['"Inter"', 'sans-serif'],
                    },
                    colors: {
                        brand: {
                            50: '#eef2ff',
                            100: '#e0e7ff',
                            500: '#6366f1', // Indigo 500
                            600: '#4f46e5', // Indigo 600
                            900: '#312e81',
                        }
                    }
                }
            }
        }
    </script>
    <style>
        /* Sembunyikan scrollbar bawaan untuk tampilan yang lebih bersih */
        .no-scrollbar::-webkit-scrollbar {
            display: none;
        }
        .no-scrollbar {
            -ms-overflow-style: none;
            scrollbar-width: none;
        }
        
        .hero-pattern {
            background-image: radial-gradient(#e0e7ff 1px, transparent 1px);
            background-size: 20px 20px;
        }
    </style>
</head>
<body class="bg-gray-50 text-gray-800 antialiased flex flex-col min-h-screen">

    <!-- Navigasi -->
    <nav class="bg-white/80 backdrop-blur-lg sticky top-0 z-40 border-b border-gray-200">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex justify-between items-center h-20">
                <!-- Logo Brand -->
                <div class="flex items-center gap-3 cursor-pointer">
                    <div class="bg-brand-600 text-white p-2.5 rounded-xl shadow-lg shadow-brand-500/30">
                        <i class="ph ph-storefront text-2xl"></i>
                    </div>
                    <div class="flex flex-col">
                        <span class="font-bold text-xl tracking-tight text-gray-900 leading-none">EgiShop</span>
                        <span class="text-[10px] font-semibold text-brand-600 uppercase tracking-widest mt-1">by M. Egi Sujana</span>
                    </div>
                </div>

                <!-- Menu Desktop -->
                <div class="hidden md:flex space-x-8">
                    <a href="#" class="text-gray-900 font-medium hover:text-brand-600 transition-colors">Beranda</a>
                    <a href="#kategori" class="text-gray-500 font-medium hover:text-brand-600 transition-colors">Koleksi</a>
                    <a href="#produk" class="text-gray-500 font-medium hover:text-brand-600 transition-colors">Produk Egi</a>
                </div>

                <!-- Tombol Aksi -->
                <div class="flex items-center gap-3 sm:gap-5">
                    <button onclick="toggleCart()" class="relative p-2 text-gray-600 hover:text-brand-600 transition-colors group">
                        <i class="ph ph-shopping-bag text-2xl group-hover:scale-110 transition-transform"></i>
                        <span id="cart-badge" class="absolute -top-1 -right-1 bg-rose-500 text-white text-[10px] font-bold h-5 w-5 rounded-full flex items-center justify-center opacity-0 scale-50 transition-all duration-300">0</span>
                    </button>
                    <button class="hidden sm:flex items-center gap-2 bg-gray-900 hover:bg-gray-800 text-white px-5 py-2.5 rounded-full font-medium text-sm transition-colors shadow-md">
                        <i class="ph ph-user text-lg"></i>
                        <span>Login</span>
                    </button>
                </div>
            </div>
        </div>
    </nav>

    <!-- Bagian Hero / Banner Utama -->
    <section class="relative pt-12 pb-20 lg:pt-20 lg:pb-28 overflow-hidden bg-white">
        <div class="absolute inset-0 hero-pattern opacity-50"></div>
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 relative z-10">
            <div class="text-center max-w-3xl mx-auto">
                <span class="inline-block py-1 px-4 rounded-full bg-brand-50 text-brand-600 font-semibold text-sm mb-6 border border-brand-100">
                    ✨ Kurasi Eksklusif 2026
                </span>
                <h1 class="text-4xl sm:text-5xl lg:text-6xl font-extrabold text-gray-900 tracking-tight mb-6">
                    Pilihan Gaya Terbaik oleh <br class="hidden sm:block"/>
                    <span class="text-transparent bg-clip-text bg-gradient-to-r from-brand-600 to-purple-600">Muhammad Egi Sujana</span>
                </h1>
                <p class="text-lg text-gray-500 mb-10 max-w-2xl mx-auto">
                    Selamat datang di toko resmi saya. Temukan berbagai produk fashion, gadget, dan gaya hidup premium yang saya pilihkan khusus untuk meningkatkan kualitas hidup Anda.
                </p>
                <div class="flex flex-col sm:flex-row gap-4 justify-center">
                    <a href="#produk" class="bg-brand-600 hover:bg-brand-500 text-white px-8 py-3.5 rounded-full font-semibold text-lg transition-transform hover:-translate-y-1 shadow-xl shadow-brand-500/30">
                        Eksplorasi Produk
                    </a>
                    <a href="#kategori" class="bg-white hover:bg-gray-50 text-gray-700 border border-gray-200 px-8 py-3.5 rounded-full font-semibold text-lg transition-all hover:shadow-md">
                        Lihat Kategori
                    </a>
                </div>
            </div>
        </div>
    </section>

    <!-- Kategori Pilihan -->
    <section id="kategori" class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
        <div class="flex flex-col md:flex-row justify-between items-end mb-8 gap-4">
            <div>
                <h2 class="text-3xl font-bold text-gray-900 mb-2">Koleksi Egi</h2>
                <p class="text-gray-500">Kategori produk yang paling sering dicari.</p>
            </div>
        </div>
        
        <div class="grid grid-cols-2 md:grid-cols-4 gap-4 sm:gap-6">
            <!-- Kategori 1 -->
            <div class="group relative overflow-hidden rounded-3xl cursor-pointer">
                <div class="aspect-w-1 aspect-h-1 w-full bg-gray-200 h-48">
                    <img src="https://images.unsplash.com/photo-1523275335684-37898b6baf30?auto=format&fit=crop&w=500&q=80" alt="" class="w-full h-full object-cover group-hover:scale-110 transition-transform duration-700">
                </div>
                <div class="absolute inset-0 bg-gradient-to-t from-gray-900/80 via-gray-900/20 to-transparent"></div>
                <div class="absolute bottom-0 left-0 p-5">
                    <h3 class="text-white font-bold text-lg">Aksesoris</h3>
                    <p class="text-gray-300 text-sm">Gaya Premium</p>
                </div>
            </div>
            <!-- Kategori 2 -->
            <div class="group relative overflow-hidden rounded-3xl cursor-pointer">
                <div class="aspect-w-1 aspect-h-1 w-full bg-gray-200 h-48">
                    <img src="https://images.unsplash.com/photo-1511499767150-a48a237f0083?auto=format&fit=crop&w=500&q=80" alt="" class="w-full h-full object-cover group-hover:scale-110 transition-transform duration-700">
                </div>
                <div class="absolute inset-0 bg-gradient-to-t from-gray-900/80 via-gray-900/20 to-transparent"></div>
                <div class="absolute bottom-0 left-0 p-5">
                    <h3 class="text-white font-bold text-lg">Fashion</h3>
                    <p class="text-gray-300 text-sm">Trend Terkini</p>
                </div>
            </div>
            <!-- Kategori 3 -->
            <div class="group relative overflow-hidden rounded-3xl cursor-pointer">
                <div class="aspect-w-1 aspect-h-1 w-full bg-gray-200 h-48">
                    <img src="https://images.unsplash.com/photo-1498049794561-7780e7231661?auto=format&fit=crop&w=500&q=80" alt="[attachment_0](attachment)" class="w-full h-full object-cover group-hover:scale-110 transition-transform duration-700">
                </div>
                <div class="absolute inset-0 bg-gradient-to-t from-gray-900/80 via-gray-900/20 to-transparent"></div>
                <div class="absolute bottom-0 left-0 p-5">
                    <h3 class="text-white font-bold text-lg">Gadget</h3>
                    <p class="text-gray-300 text-sm">Teknologi Baru</p>
                </div>
            </div>
            <!-- Kategori 4 -->
            <div class="group relative overflow-hidden rounded-3xl cursor-pointer">
                <div class="aspect-w-1 aspect-h-1 w-full bg-gray-200 h-48">
                    <img src="https://images.unsplash.com/photo-1542291026-7eec264c27ff?auto=format&fit=crop&w=500&q=80" alt="" class="w-full h-full object-cover group-hover:scale-110 transition-transform duration-700">
                </div>
                <div class="absolute inset-0 bg-gradient-to-t from-gray-900/80 via-gray-900/20 to-transparent"></div>
                <div class="absolute bottom-0 left-0 p-5">
                    <h3 class="text-white font-bold text-lg">Sepatu</h3>
                    <p class="text-gray-300 text-sm">Koleksi Sneaker</p>
                </div>
            </div>
        </div>
    </section>

    <!-- Daftar Produk -->
    <section id="produk" class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12 mb-20">
        <div class="flex justify-between items-end mb-8">
            <h2 class="text-3xl font-bold text-gray-900">Spesial Pilihan Egi</h2>
            <button class="text-brand-600 font-semibold hover:text-brand-800 text-sm hidden sm:flex items-center gap-1">
                Lihat Semua <i class="ph ph-arrow-right"></i>
            </button>
        </div>
        
        <!-- Wadah Produk -->
        <div id="product-container" class="grid grid-cols-2 lg:grid-cols-4 gap-4 sm:gap-8">
            <!-- Produk akan dimuat ke sini melalui JavaScript -->
        </div>
    </section>

    <!-- Footer -->
    <footer class="bg-gray-900 pt-16 pb-8 mt-auto border-t border-gray-800">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="grid grid-cols-1 md:grid-cols-12 gap-8 md:gap-12 mb-12">
                <div class="col-span-1 md:col-span-5">
                    <div class="flex items-center gap-3 mb-6">
                        <div class="bg-brand-500 text-white p-2 rounded-xl">
                            <i class="ph ph-storefront text-xl"></i>
                        </div>
                        <span class="font-bold text-2xl text-white">EgiShop</span>
                    </div>
                    <p class="text-gray-400 text-sm leading-relaxed mb-6">
                        Menghadirkan pengalaman belanja online yang personal dan terpercaya. Dikurasi langsung oleh Muhammad Egi Sujana untuk memastikan Anda mendapatkan kualitas produk terbaik.
                    </p>
                    <div class="flex space-x-4">
                        <a href="#" class="w-10 h-10 rounded-full bg-gray-800 flex items-center justify-center text-gray-400 hover:bg-brand-500 hover:text-white transition-all"><i class="ph ph-instagram-logo text-xl"></i></a>
                        <a href="#" class="w-10 h-10 rounded-full bg-gray-800 flex items-center justify-center text-gray-400 hover:bg-brand-500 hover:text-white transition-all"><i class="ph ph-twitter-logo text-xl"></i></a>
                        <a href="#" class="w-10 h-10 rounded-full bg-gray-800 flex items-center justify-center text-gray-400 hover:bg-brand-500 hover:text-white transition-all"><i class="ph ph-youtube-logo text-xl"></i></a>
                    </div>
                </div>
                
                <div class="col-span-1 md:col-span-3">
                    <h3 class="font-bold text-white mb-5">Layanan</h3>
                    <ul class="space-y-3 text-sm text-gray-400">
                        <li><a href="#" class="hover:text-brand-400 transition-colors">Cara Pemesanan</a></li>
                        <li><a href="#" class="hover:text-brand-400 transition-colors">Lacak Pesanan</a></li>
                        <li><a href="#" class="hover:text-brand-400 transition-colors">Pengembalian Dana</a></li>
                        <li><a href="#" class="hover:text-brand-400 transition-colors">FAQ</a></li>
                    </ul>
                </div>

                <div class="col-span-1 md:col-span-4">
                    <h3 class="font-bold text-white mb-5">Berlangganan Newsletter</h3>
                    <p class="text-gray-400 text-sm mb-4">Dapatkan info produk terbaru dan promo khusus dari Egi.</p>
                    <div class="flex gap-2">
                        <input type="email" placeholder="Email Anda..." class="bg-gray-800 text-white px-4 py-2.5 rounded-xl text-sm w-full outline-none focus:ring-2 focus:ring-brand-500 border border-gray-700">
                        <button class="bg-brand-600 hover:bg-brand-500 text-white px-4 py-2.5 rounded-xl font-medium transition-colors">
                            Kirim
                        </button>
                    </div>
                </div>
            </div>
            
            <div class="border-t border-gray-800 pt-8 flex flex-col md:flex-row justify-between items-center gap-4 text-sm text-gray-500">
                <p>&copy; 2026 Muhammad Egi Sujana. Hak Cipta Dilindungi.</p>
                <div class="flex gap-4">
                    <a href="#" class="hover:text-white transition-colors">Syarat & Ketentuan</a>
                    <a href="#" class="hover:text-white transition-colors">Kebijakan Privasi</a>
                </div>
            </div>
        </div>
    </footer>

    <!-- Overlay Latar Belakang Keranjang -->
    <div id="cart-overlay" class="fixed inset-0 bg-gray-900/60 backdrop-blur-sm z-50 hidden opacity-0 transition-opacity duration-300" onclick="toggleCart()"></div>
    
    <!-- Panel Keranjang Samping (Sidebar) -->
    <div id="cart-sidebar" class="fixed top-0 right-0 h-full w-full sm:w-[420px] bg-white z-50 transform translate-x-full transition-transform duration-300 ease-in-out shadow-2xl flex flex-col rounded-l-3xl sm:rounded-l-[2rem]">
        
        <!-- Header Keranjang -->
        <div class="p-6 border-b border-gray-100 flex justify-between items-center">
            <h2 class="text-xl font-bold text-gray-900 flex items-center gap-3">
                <div class="bg-brand-50 text-brand-600 p-2 rounded-lg">
                    <i class="ph ph-shopping-bag text-xl"></i>
                </div>
                Keranjang Anda
            </h2>
            <button onclick="toggleCart()" class="w-10 h-10 flex items-center justify-center text-gray-400 hover:text-gray-900 hover:bg-gray-100 rounded-full transition-colors">
                <i class="ph ph-x text-xl font-bold"></i>
            </button>
        </div>

        <!-- Daftar Item di Keranjang -->
        <div id="cart-items" class="flex-1 overflow-y-auto p-6 space-y-4 no-scrollbar bg-gray-50">
            <!-- Tampilan Kosong Default (Digantikan JS nanti) -->
        </div>

        <!-- Footer Keranjang (Total & Checkout) -->
        <div class="p-6 bg-white border-t border-gray-100 rounded-bl-3xl sm:rounded-bl-[2rem]">
            <div class="flex justify-between items-center mb-6">
                <span class="text-gray-500 font-medium text-lg">Total Pembayaran</span>
                <span id="cart-total" class="text-2xl font-black text-brand-600">Rp 0</span>
            </div>
            <button onclick="checkout()" class="w-full bg-gray-900 hover:bg-brand-600 text-white font-bold py-4 rounded-2xl shadow-lg transition-all active:scale-[0.98] flex justify-center items-center gap-2 group">
                Checkout Pesanan <i class="ph ph-arrow-right font-bold group-hover:translate-x-1 transition-transform"></i>
            </button>
        </div>
    </div>

    <!-- Wadah Notifikasi / Toast -->
    <div id="toast-container" class="fixed bottom-6 left-1/2 transform -translate-x-1/2 z-[60] flex flex-col gap-3 w-full max-w-xs sm:max-w-sm pointer-events-none px-4"></div>

    <!-- Logika Aplikasi (JavaScript) -->
    <script>
        // Data Produk Khusus "EgiShop"
        const products = [
            {
                id: 1,
                name: "Egi Signature Watch Pro",
                price: 2450000,
                image: "https://images.unsplash.com/photo-1523275335684-37898b6baf30?auto=format&fit=crop&w=500&q=80",
                category: "Aksesoris",
                badge: "Terlaris"
            },
            {
                id: 2,
                name: "Kemeja Flannel Premium",
                price: 320000,
                image: "https://images.unsplash.com/photo-1596755094514-f87e32f85e2c?auto=format&fit=crop&w=500&q=80",
                category: "Fashion",
                badge: ""
            },
            {
                id: 3,
                name: "Sneakers Urban Egi Edition",
                price: 950000,
                image: "https://images.unsplash.com/photo-1542291026-7eec264c27ff?auto=format&fit=crop&w=500&q=80",
                category: "Sepatu",
                badge: "Terbaru"
            },
            {
                id: 4,
                name: "Backpack Minimalist X",
                price: 550000,
                image: "https://images.unsplash.com/photo-1553062407-98eeb64c6a62?auto=format&fit=crop&w=500&q=80",
                category: "Aksesoris",
                badge: ""
            },
            {
                id: 5,
                name: "TWS Earbuds Pro 2",
                price: 1100000,
                image: "https://images.unsplash.com/photo-1590658268037-6bf12165a8df?auto=format&fit=crop&w=500&q=80",
                category: "Gadget",
                badge: "Promo"
            },
            {
                id: 6,
                name: "Kacamata Hitam Klasik",
                price: 275000,
                image: "https://images.unsplash.com/photo-1511499767150-a48a237f0083?auto=format&fit=crop&w=500&q=80",
                category: "Fashion",
                badge: ""
            },
            {
                id: 7,
                name: "Jaket Denim Egi Choice",
                price: 680000,
                image: "https://images.unsplash.com/photo-1576871337622-98d48d1cf531?auto=format&fit=crop&w=500&q=80",
                category: "Fashion",
                badge: "Terlaris"
            },
            {
                id: 8,
                name: "Dompet Kulit Asli",
                price: 450000,
                image: "https://images.unsplash.com/photo-1627123424574-724758594e93?auto=format&fit=crop&w=500&q=80",
                category: "Aksesoris",
                badge: ""
            }
        ];

        // State Penyimpanan Data Keranjang
        let cart = [];
        let isCartOpen = false;

        // Fungsi Format Rupiah
        const formatRupiah = (number) => {
            return new Intl.NumberFormat('id-ID', { style: 'currency', currency: 'IDR', minimumFractionDigits: 0 }).format(number);
        };

        // Fungsi Menampilkan Notifikasi (Toast)
        const showToast = (message) => {
            const container = document.getElementById('toast-container');
            const toast = document.createElement('div');
            
            toast.className = 'bg-gray-900 text-white px-5 py-4 rounded-2xl shadow-2xl text-sm font-medium flex items-center gap-3 transform translate-y-12 opacity-0 transition-all duration-400 ease-out pointer-events-auto border border-gray-700';
            toast.innerHTML = `
                <div class="bg-green-500/20 text-green-400 p-1 rounded-full flex-shrink-0">
                    <i class="ph ph-check text-lg"></i>
                </div>
                <span>${message}</span>
            `;
            
            container.appendChild(toast);
            
            // Animasi Masuk
            requestAnimationFrame(() => {
                toast.classList.remove('translate-y-12', 'opacity-0');
            });

            // Hilang otomatis setelah 3 detik
            setTimeout(() => {
                toast.classList.add('translate-y-12', 'opacity-0');
                setTimeout(() => toast.remove(), 400);
            }, 3000);
        };

        // Render Katalog Produk
        const renderProducts = () => {
            const container = document.getElementById('product-container');
            container.innerHTML = '';

            products.forEach(product => {
                const badgeHTML = product.badge 
                    ? `<div class="absolute top-3 left-3 bg-brand-600 text-white px-3 py-1 rounded-full text-[10px] font-bold uppercase tracking-wider shadow-md z-10">${product.badge}</div>` 
                    : '';

                const card = document.createElement('div');
                card.className = 'bg-white rounded-3xl overflow-hidden shadow-sm border border-gray-100 hover:shadow-xl transition-all duration-300 flex flex-col group';
                
                card.innerHTML = `
                    <div class="relative w-full pt-[100%] bg-gray-50 overflow-hidden">
                        ${badgeHTML}
                        <img src="${product.image}" alt="" class="absolute inset-0 w-full h-full object-cover group-hover:scale-105 transition-transform duration-500">
                        <button onclick="addToCart(${product.id})" class="absolute bottom-3 right-3 bg-white/90 backdrop-blur text-gray-900 w-10 h-10 rounded-full flex justify-center items-center shadow-lg transform translate-y-12 opacity-0 group-hover:translate-y-0 group-hover:opacity-100 transition-all duration-300 hover:bg-brand-600 hover:text-white">
                            <i class="ph ph-plus font-bold text-xl"></i>
                        </button>
                    </div>
                    <div class="p-5 flex flex-col flex-1">
                        <span class="text-[11px] font-bold text-gray-400 mb-2 uppercase tracking-widest">${product.category}</span>
                        <h3 class="text-sm sm:text-base font-semibold text-gray-900 leading-snug mb-3 flex-1 group-hover:text-brand-600 transition-colors">${product.name}</h3>
                        <div class="flex items-center justify-between mt-auto">
                            <span class="font-bold text-lg text-gray-900">${formatRupiah(product.price)}</span>
                        </div>
                        <button onclick="addToCart(${product.id})" class="mt-4 w-full bg-gray-50 hover:bg-brand-50 text-brand-600 font-semibold py-2.5 rounded-xl text-sm transition-colors flex justify-center items-center gap-2 lg:hidden">
                            <i class="ph ph-shopping-cart"></i> Beli
                        </button>
                    </div>
                `;
                container.appendChild(card);
            });
        };

        // Fungsi Tambah ke Keranjang
        const addToCart = (productId) => {
            const product = products.find(p => p.id === productId);
            const existingItem = cart.find(item => item.id === productId);

            if (existingItem) {
                existingItem.quantity += 1;
            } else {
                cart.push({ ...product, quantity: 1 });
            }

            updateCartUI();
            showToast(`<b>${product.name}</b> dimasukkan keranjang!`);
        };

        // Fungsi Hapus dari Keranjang
        const removeFromCart = (productId) => {
            cart = cart.filter(item => item.id !== productId);
            updateCartUI();
        };

        // Fungsi Update Jumlah Barang
        const updateQuantity = (productId, delta) => {
            const item = cart.find(item => item.id === productId);
            if (item) {
                item.quantity += delta;
                if (item.quantity <= 0) {
                    removeFromCart(productId);
                } else {
                    updateCartUI();
                }
            }
        };

        // Perbarui Tampilan Keranjang (Sidebar & Badge)
        const updateCartUI = () => {
            const totalItems = cart.reduce((sum, item) => sum + item.quantity, 0);
            const badge = document.getElementById('cart-badge');
            
            // Animasi Badge Buble
            if (totalItems > 0) {
                badge.textContent = totalItems;
                badge.classList.remove('opacity-0', 'scale-50');
                badge.classList.add('scale-100');
                
                // Pop effect
                badge.classList.add('scale-125');
                setTimeout(() => badge.classList.remove('scale-125'), 150);
            } else {
                badge.classList.add('opacity-0', 'scale-50');
                badge.classList.remove('scale-100');
            }

            // Update Daftar Item Keranjang
            const container = document.getElementById('cart-items');
            
            if (cart.length === 0) {
                container.innerHTML = `
                    <div class="flex flex-col items-center justify-center h-full text-gray-400 gap-4 mt-20">
                        <div class="w-24 h-24 bg-gray-100 rounded-full flex items-center justify-center">
                            <i class="ph ph-shopping-bag-open text-5xl text-gray-300"></i>
                        </div>
                        <p class="font-medium text-gray-500">Keranjang masih kosong nih.</p>
                        <button onclick="toggleCart()" class="mt-2 text-brand-600 font-semibold text-sm bg-brand-50 px-6 py-2.5 rounded-full hover:bg-brand-100 transition-colors">Belanja Dulu Yuk</button>
                    </div>
                `;
            } else {
                container.innerHTML = cart.map(item => `
                    <div class="bg-white p-4 rounded-2xl flex gap-4 shadow-sm border border-gray-100 relative group">
                        <div class="w-20 h-20 bg-gray-100 rounded-xl overflow-hidden flex-shrink-0">
                            <img src="${item.image}" alt="" class="w-full h-full object-cover">
                        </div>
                        <div class="flex-1 flex flex-col justify-between">
                            <div class="pr-6">
                                <h4 class="text-sm font-semibold text-gray-900 leading-tight mb-1">${item.name}</h4>
                                <span class="font-bold text-sm text-brand-600">${formatRupiah(item.price)}</span>
                            </div>
                            <div class="flex justify-between items-center mt-2">
                                <div class="flex items-center bg-gray-50 border border-gray-200 rounded-lg overflow-hidden">
                                    <button onclick="updateQuantity(${item.id}, -1)" class="w-8 h-8 flex items-center justify-center text-gray-500 hover:bg-gray-200 hover:text-gray-900 transition-colors font-medium">-</button>
                                    <span class="text-xs font-bold w-6 text-center text-gray-700">${item.quantity}</span>
                                    <button onclick="updateQuantity(${item.id}, 1)" class="w-8 h-8 flex items-center justify-center text-gray-500 hover:bg-gray-200 hover:text-gray-900 transition-colors font-medium">+</button>
                                </div>
                            </div>
                        </div>
                        <button onclick="removeFromCart(${item.id})" class="absolute top-4 right-4 text-gray-300 hover:text-rose-500 bg-white rounded-full p-1 transition-colors">
                            <i class="ph ph-trash text-lg"></i>
                        </button>
                    </div>
                `).join('');
            }

            // Update Total Harga
            const totalPrice = cart.reduce((sum, item) => sum + (item.price * item.quantity), 0);
            document.getElementById('cart-total').textContent = formatRupiah(totalPrice);
        };

        // Fungsi Buka/Tutup Keranjang
        const toggleCart = () => {
            const overlay = document.getElementById('cart-overlay');
            const sidebar = document.getElementById('cart-sidebar');
            const body = document.body;

            isCartOpen = !isCartOpen;

            if (isCartOpen) {
                overlay.classList.remove('hidden');
                // Timeout singkat agar display block teraplikasi sebelum transisi opacity
                setTimeout(() => overlay.classList.remove('opacity-0'), 10);
                sidebar.classList.remove('translate-x-full');
                body.style.overflow = 'hidden'; // Mengunci scroll body
            } else {
                overlay.classList.add('opacity-0');
                sidebar.classList.add('translate-x-full');
                body.style.overflow = '';
                setTimeout(() => overlay.classList.add('hidden'), 300);
            }
        };

        // Simulasi Checkout
        const checkout = () => {
            if (cart.length === 0) {
                showToast("Oops! Keranjang belanja masih kosong.");
                return;
            }
            
            // Tutup sidebar lalu tampilkan simulasi loading
            toggleCart();
            showToast("Memproses pesanan Anda... 🚀");
            
            // Kosongkan keranjang (Simulasi setelah berhasil dibayar)
            setTimeout(() => {
                cart = [];
                updateCartUI();
                showToast("Pesanan Berhasil Dibuat! 🎉");
            }, 2500);
        };

        // Inisialisasi awal saat halaman dimuat
        document.addEventListener('DOMContentLoaded', () => {
            renderProducts();
            updateCartUI();
        });

    </script>
</body>
</html>


```
